# Copy this file to .env on the Ubuntu server.
# Generate the key with: openssl rand -hex 32
BUBBLE_API_KEY=change-this-key

# Nginx connects to this loopback-only port.
WEB_BIND_ADDRESS=127.0.0.1
WEB_PORT=3100
